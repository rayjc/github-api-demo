version: '3'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: github-api-demo
    container_name: github-api-demo
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
    networks:
      - backend
    ports:
      - "8080:8080"
    environment:
      - APP_PORT=8080


networks:
  backend: